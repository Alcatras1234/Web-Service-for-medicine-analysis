fastapi
uvicorn[standard]
numpy<2
opencv-python-headless
pillow
pydantic

# Triton gRPC client — модель крутится на удалённом Triton по адресу из TRITON_URL.
tritonclient[grpc]==2.42.0

# Prometheus метрики через /metrics — латенси /infer_batch, число запросов, etc.
prometheus-fastapi-instrumentator==7.0.0
